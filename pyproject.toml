[project]
name = "settlers"
version = "2025.3.0"
requires-python = ">= 3.12"
dependencies = [
    "colorama",
    "names",
    "pysdl2",
    "structlog",
    "pillow"
]

[project.optional-dependencies]
dev = [
    "black",
    "flake8",
]
